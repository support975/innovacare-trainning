import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.innovacare.training',
  appName: 'Innovacare Training',
  webDir: 'dist/myapp/browser'
};

export default config;
